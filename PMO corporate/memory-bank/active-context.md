# Active Context

## Ongoing Tasks
- [x] Initialise the workspace Memory Bank (`memory-bank/`).
- [x] Onboard and read Serena Instruction Manual rules.
- [x] Standardise the circular back button design globally using `.btn-back-circle` inside `dashboard-styles.css`.
- [x] Integrate dynamic headers in `authorise_redemption.html` linking seamlessly with parameters from list pages.
- [x] Restructure Fund Analytics as a top-level standalone module selectable from global navigation pills.
- [ ] Document project rules (`.cursorrules` and `.clinerules`) for coding assistants.
- [ ] Build a custom workspace skill (`skills/pmo-design/`) representing the "Pro Max" corporate design system.

## Known Issues
- **No Build Pipeline**: Currently a static frontend prototype, meaning assets must be referenced directly with absolute or relative paths. There is no automated bundling or CSS compilation.

## Next Steps
1. **Create Rules Files**: Write `.cursorrules` and `.clinerules` to the workspace root to guide formatting and components.
2. **Implement Skill Package**: Create the `skills/pmo-design/SKILL.md` and reference files to provide standard layouts, classes, and templates for creating new portal screens.
3. **Verify Interactive Elements**: Open local web browser instances to enjoy the premium hover-glow back arrow animations in all detail-focused dashboards!
